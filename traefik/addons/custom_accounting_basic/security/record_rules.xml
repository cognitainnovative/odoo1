<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Odoo's account module already has strong record rules — no additions needed for M5 -->

    <record id="rule_recurring_invoice_template_company" model="ir.rule">
        <field name="name">Recurring Invoice Template: company isolation</field>
        <field name="model_id" ref="model_recurring_invoice_template"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
    <record id="rule_accounting_audit_log_company" model="ir.rule">
        <field name="name">Accounting Audit Log: company isolation</field>
        <field name="model_id" ref="model_accounting_audit_log"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>
