<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="rule_helpdesk_ticket_company" model="ir.rule">
        <field name="name">Helpdesk Ticket: company scope</field>
        <field name="model_id" ref="model_helpdesk_ticket"/>
        <field name="domain_force">[('company_id','in',company_ids)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Portal customers see only their own tickets -->
    <record id="rule_helpdesk_ticket_portal" model="ir.rule">
        <field name="name">Helpdesk Ticket: portal own tickets only</field>
        <field name="model_id" ref="model_helpdesk_ticket"/>
        <field name="domain_force">[('partner_id','=',user.partner_id.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
    </record>

    <record id="rule_helpdesk_team_company" model="ir.rule">
        <field name="name">Helpdesk Team: company isolation</field>
        <field name="model_id" ref="model_helpdesk_team"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>
</odoo>
