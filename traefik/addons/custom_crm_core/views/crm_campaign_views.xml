<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_crm_campaign_list" model="ir.ui.view">
        <field name="name">crm.campaign.list</field>
        <field name="model">crm.campaign</field>
        <field name="arch" type="xml">
            <list string="Campaigns"
                  decoration-success="state == 'active'"
                  decoration-muted="state == 'done'">
                <field name="name"/>
                <field name="channel"/>
                <field name="state" widget="badge"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="lead_count" string="Leads"/>
                <field name="won_count" string="Won"/>
                <field name="total_revenue" string="Revenue (€)" widget="monetary"
                       options="{'currency_field': False}"/>
                <field name="budget" string="Budget (€)"/>
            </list>
        </field>
    </record>

    <record id="view_crm_campaign_form" model="ir.ui.view">
        <field name="name">crm.campaign.form</field>
        <field name="model">crm.campaign</field>
        <field name="arch" type="xml">
            <form string="Campaign">
                <header>
                    <button name="action_activate" string="Activate" type="object"
                            class="btn-primary" invisible="state == 'active'"/>
                    <button name="action_done" string="Mark Done" type="object"
                            class="btn-secondary" invisible="state not in ('active', 'paused')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,done"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" icon="fa-users" type="object"
                                name="action_view_leads">
                            <field name="lead_count" widget="statinfo" string="Leads"/>
                        </button>
                        <button class="oe_stat_button" icon="fa-trophy" type="object"
                                name="action_view_leads">
                            <field name="won_count" widget="statinfo" string="Won"/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="channel"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                        <group>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="budget"/>
                            <field name="target_leads"/>
                        </group>
                    </group>
                    <group string="UTM Tracking">
                        <group>
                            <field name="utm_source"/>
                            <field name="utm_medium"/>
                            <field name="utm_campaign"/>
                        </group>
                    </group>
                    <group string="Description">
                        <field name="description" nolabel="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_crm_campaign" model="ir.actions.act_window">
        <field name="name">Campaigns</field>
        <field name="res_model">crm.campaign</field>
        <field name="view_mode">list,form</field>
        <field name="context">{"search_default_active": 1}</field>
    </record>

</odoo>
