[options]
; ── Database ──────────────────────────────────────────────────────────────────
db_host = False
db_port = False
db_user = diviner
db_password = False
db_name = tf_v19com

; ── Paths ─────────────────────────────────────────────────────────────────────
addons_path = /home/diviner/Odoo/19/addons,/home/diviner/Odoo/custom/traefik/addons
data_dir = /home/diviner/Odoo/custom/traefik/.odoo_data

; ── Server ────────────────────────────────────────────────────────────────────
http_port = 8069
workers = 0
max_cron_threads = 1

; ── Dev / Debug ───────────────────────────────────────────────────────────────
dev_mode = all
log_level = info
log_handler = :INFO
logfile = /home/diviner/Odoo/custom/traefik/logs/odoo.log

; ── Performance ───────────────────────────────────────────────────────────────
limit_memory_hard = 2684354560
limit_memory_soft = 2147483648
limit_time_cpu = 600
limit_time_real = 1200

; ── Security ──────────────────────────────────────────────────────────────────
admin_passwd = dev_admin_change_in_prod
list_db = True
